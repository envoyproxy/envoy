dynamic_resources:
  lds_config:
    path: {{ lds_json_path }}
  cds_config:
    path: {{ cds_json_path }}
admin:
  access_log_path: {{ null_device_path }}
  address:
    socket_address:
      address: {{ ntop_ip_loopback_address }}
      port_value: 0
