config {
  name: "RegexNoMatch"
  virtual_hosts {
    name: "regex"
    domains: "regex.lyft.com"
    routes {
      match {
        regex: "/regex"
      }
      route {
        cluster: "some-cluster"
      }
    }
  }
}
headers {
  headers {
    key: ":authority"
    value: "regex.lyft.com"
  }
  headers {
    key: ":path"
    value: "/regex"
  }
  headers {
    key: "x-forwarded-proto"
    value: "https"
  }
}
