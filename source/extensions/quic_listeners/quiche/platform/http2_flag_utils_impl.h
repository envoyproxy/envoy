#pragma once

// NOLINT(namespace-envoy)

#define HTTP2_RELOADABLE_FLAG_COUNT_IMPL(flag)                                                     \
  do {                                                                                             \
  } while (0)
