#pragma once

// NOLINT(namespace-envoy)

// This is a convenience header that includes the main dynamic modules ABI.
// All cluster-related types, event hooks, and callbacks are defined in the main abi.h file.

#include "source/extensions/dynamic_modules/abi.h"
