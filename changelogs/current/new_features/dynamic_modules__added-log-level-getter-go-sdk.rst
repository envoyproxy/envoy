Expose ``envoy_dynamic_module_callback_get_log_level`` in the Go HTTP filter and filter config handles as
``GetLogLevel`` and and ``envoy_dynamic_module_callback_log_enabled`` as ``IsLogLevelEnabled``.
