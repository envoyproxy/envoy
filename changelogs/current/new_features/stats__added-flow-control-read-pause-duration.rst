Added ``downstream_flow_control_combined_reading_delay_micros`` and
``upstream_flow_control_combined_reading_delay_micros`` counters to report the total
microseconds accumulated across streams or connections while flow-control read pauses
are active.
