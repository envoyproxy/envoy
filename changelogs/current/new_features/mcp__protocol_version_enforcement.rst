Added ``protocol_versions`` support for enforcing configured MCP protocol revisions, including ``2026-07-28``-specific header and transport semantics, protocol-version consistency checks between ``MCP-Protocol-Version`` and request metadata, and Base64 decoding for encoded ``Mcp-Name`` values. Added ``error_reply_format`` support for returning MCP protocol errors as standardized JSON-RPC error responses while preserving the existing plain-text reply format by default.
