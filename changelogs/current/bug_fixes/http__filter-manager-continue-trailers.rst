http: fixed a bug where HTTP decoder or encoder filters continuing from paused headers or data callbacks could prematurely dispatch stream trailers and terminate the filter chain before earlier filters in the chain completed processing subsequent body chunks and trailers. This behavior can be temporarily reverted by setting runtime feature ``envoy.reloadable_features.filter_manager_continue_trailers_only_when_end_stream`` to ``false``.
