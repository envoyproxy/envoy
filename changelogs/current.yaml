date: Pending

behavior_changes:
# *Changes that are expected to cause an incompatibility if applicable; deployment changes are likely required*

minor_behavior_changes:
# *Changes that may cause incompatibilities for some users, but should not for most*

bug_fixes:
# *Changes expected to improve the state of the world and are unlikely to have negative effects*

removed_config_or_runtime:
# *Normally occurs at the end of the* :ref:`deprecation period <deprecated>`

new_features:
- area: stateful_session
  change: |
    Supports packages mode in header based stateful session extension to keep the exist session header value
    from upstream server. See :ref:`packages_mode_config
    <envoy_v3_api_field_extensions.http.stateful_session.header.v3.HeaderBasedSessionState.packages_mode_config>`
    for more details.

deprecated:
