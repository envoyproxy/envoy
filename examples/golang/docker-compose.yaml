version: "3.8"
services:

  proxy:
    build:
      context: .
      dockerfile: Dockerfile-proxy
    depends_on:
    - web_service
    ports:
    - "${PORT_PROXY:-10000}:10000"

  web_service:
    environment:
      - PORT=9000
    build:
      context: ../shared/echo
