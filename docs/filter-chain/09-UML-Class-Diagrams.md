# Part 9: UML Class Diagrams

## Table of Contents
1. [Introduction](#introduction)
2. [Factory Context Class Hierarchy](#factory-context-class-hierarchy)
3. [Filter Interface Hierarchy](#filter-interface-hierarchy)
4. [Filter Factory Classes](#filter-factory-classes)
5. [Filter Chain Classes](#filter-chain-classes)
6. [Filter Manager Classes](#filter-manager-classes)
7. [Registry System Classes](#registry-system-classes)
8. [Complete System Overview](#complete-system-overview)

## Introduction

This document provides UML class diagrams showing the relationships between key classes in Envoy's filter chain system. The diagrams use ASCII art for portability.

## Factory Context Class Hierarchy

### Context Inheritance Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                  FACTORY CONTEXT HIERARCHY                  │
└─────────────────────────────────────────────────────────────┘

                    ┌────────────────────────────┐
                    │ CommonFactoryContext       │
                    │ (interface)                │
                    ├────────────────────────────┤
                    │ + api(): Api&              │
                    │ + clusterManager(): CM&    │
                    │ + threadLocal(): TLS&      │
                    │ + store(): Stats::Store&   │
                    │ + runtime(): Runtime&      │
                    │ + singletonManager(): SM&  │
                    │ + timeSource(): TS&        │
                    └──────────────┬─────────────┘
                                   │
                                   │ extends
                                   ▼
                    ┌────────────────────────────┐
                    │ ServerFactoryContext       │
                    │ (interface)                │
                    ├────────────────────────────┤
                    │ + httpContext(): HC&       │
                    │ + routerContext(): RC&     │
                    │ + grpcContext(): GC&       │
                    │ + overloadManager(): OM&   │
                    │ + configTracker(): CT&     │
                    └──────────────┬─────────────┘
                                   │
                                   │ extends
                                   ▼
                    ┌────────────────────────────┐
                    │ GenericFactoryContext      │
                    │ (interface)                │
                    ├────────────────────────────┤
                    │ + initManager(): IM&       │
                    │ + scope(): Stats::Scope&   │
                    └──────────────┬─────────────┘
                                   │
                                   │ extends
                                   ▼
                    ┌────────────────────────────┐
                    │ FactoryContext             │
                    │ (interface)                │
                    ├────────────────────────────┤
                    │ + drainDecision(): DD&     │
                    │ + listenerInfo(): LI&      │
                    │ + listenerScope(): Scope&  │
                    │ + getFilterChainFC(): FC&  │
                    └──────────────┬─────────────┘
                        │                    │
                        │                    │ extends
                        │                    ▼
                        │         ┌────────────────────────────┐
                        │         │ ListenerFactoryContext     │
                        │         │ (interface)                │
                        │         ├────────────────────────────┤
                        │         │ + listenerFilterMatcher()  │
                        │         └────────────────────────────┘
                        │
                        │ uses
                        ▼
        ┌───────────────────────────────────┐
        │ FilterChainFactoryContext         │
        │ (interface)                       │
        ├───────────────────────────────────┤
        │ + initManager(): InitManager&     │
        │ + scope(): Stats::Scope&          │
        │ + startDraining(): void           │
        │ + isDraining(): bool              │
        └───────────────┬───────────────────┘
                        │
                        │ implemented by
                        ▼
        ┌───────────────────────────────────┐
        │ PerFilterChainFactoryContextImpl  │
        │ (concrete)                        │
        ├───────────────────────────────────┤
        │ - parent_context_: FC&            │
        │ - init_manager_: InitManager&     │
        │ - stats_scope_: Stats::Scope&     │
        │ - draining_: std::atomic<bool>    │
        ├───────────────────────────────────┤
        │ + initManager(): InitManager&     │
        │ + scope(): Stats::Scope&          │
        │ + startDraining(): void           │
        │ + isDraining(): bool              │
        └───────────────────────────────────┘

LEGEND:
───────  Inheritance/Extension
┄┄┄┄┄┄┄  Association/Usage
```

## Filter Interface Hierarchy

### Network Filter Interfaces

```
┌─────────────────────────────────────────────────────────────┐
│                 NETWORK FILTER INTERFACES                   │
└─────────────────────────────────────────────────────────────┘

                    ┌──────────────────────────┐
                    │ ReadFilter               │
                    │ (interface)              │
                    ├──────────────────────────┤
                    │ + onNewConnection(): FS  │
                    │ + onData(buf, end): FS   │
                    │ + initializeReadFC(): v  │
                    └────────────┬─────────────┘
                                 │
                                 │
        ┌────────────────────────┼────────────────────────┐
        │                        │                        │
        │                        │                        │
        ▼                        ▼                        ▼
┌───────────────┐    ┌──────────────────┐    ┌──────────────────┐
│ WriteFilter   │    │ Filter           │    │ ListenerFilter   │
│ (interface)   │    │ (interface)      │    │ (interface)      │
├───────────────┤    ├──────────────────┤    ├──────────────────┤
│ + onWrite():  │    │ Extends both     │    │ + onAccept(): FS │
│     FS        │    │ ReadFilter &     │    │ + onData(): FS   │
│ + initWFC():  │    │ WriteFilter      │    └──────────────────┘
│     void      │    └──────────────────┘
└───────────────┘

            FilterStatus enum:
            ┌──────────────────────────┐
            │ + Continue               │
            │ + StopIteration          │
            └──────────────────────────┘


CONCRETE IMPLEMENTATIONS:

    ┌────────────────────────────────────────────┐
    │ TcpProxyFilter                             │
    │ (concrete, implements Filter)              │
    ├────────────────────────────────────────────┤
    │ - config_: ConfigSharedPtr                 │
    │ - cluster_manager_: ClusterManager&        │
    │ - upstream_conn_: ConnectionPtr            │
    │ - read_callbacks_: ReadFilterCallbacks*    │
    │ - write_callbacks_: WriteFilterCallbacks*  │
    ├────────────────────────────────────────────┤
    │ + onNewConnection(): FilterStatus          │
    │ + onData(buffer, end): FilterStatus        │
    │ + onWrite(buffer, end): FilterStatus       │
    │ + initializeReadFilterCallbacks(cb): void  │
    │ + initializeWriteFilterCallbacks(cb): void │
    └────────────────────────────────────────────┘

    ┌────────────────────────────────────────────┐
    │ HttpConnectionManagerImpl                  │
    │ (concrete, implements ReadFilter)          │
    ├────────────────────────────────────────────┤
    │ - config_: HttpConnectionManagerConfig     │
    │ - codec_: HttpCodec*                       │
    │ - active_streams_: list<ActiveStream>      │
    │ - filter_chain_factory_: FCFactory         │
    ├────────────────────────────────────────────┤
    │ + onNewConnection(): FilterStatus          │
    │ + onData(buffer, end): FilterStatus        │
    │ + createFilterChain(cb): bool              │
    └────────────────────────────────────────────┘

    ┌────────────────────────────────────────────┐
    │ TlsInspector                               │
    │ (concrete, implements ListenerFilter)      │
    ├────────────────────────────────────────────┤
    │ - config_: TlsInspectorConfig              │
    │ - callbacks_: ListenerFilterCallbacks*     │
    ├────────────────────────────────────────────┤
    │ + onAccept(): FilterStatus                 │
    │ + onData(buffer): FilterStatus             │
    └────────────────────────────────────────────┘
```

### HTTP Filter Interfaces

```
┌─────────────────────────────────────────────────────────────┐
│                   HTTP FILTER INTERFACES                    │
└─────────────────────────────────────────────────────────────┘

        ┌──────────────────────────────────────┐
        │ StreamDecoderFilter                  │
        │ (interface)                          │
        ├──────────────────────────────────────┤
        │ + decodeHeaders(h, end): FHS         │
        │ + decodeData(buf, end): FDS          │
        │ + decodeTrailers(t): FTS             │
        │ + decodeMetadata(m): void            │
        │ + setDecoderFilterCallbacks(cb): v   │
        └──────────────┬───────────────────────┘
                       │
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ▼              ▼              ▼
┌───────────────┐  ┌──────────────────────────┐  ┌────────────┐
│StreamEncoder  │  │ StreamFilter             │  │ Filter     │
│Filter         │  │ (interface)              │  │Metadata    │
│(interface)    │  ├──────────────────────────┤  │            │
├───────────────┤  │ Extends both             │  └────────────┘
│ + encodeHdr   │  │ StreamDecoderFilter &    │
│   (h,end):FHS │  │ StreamEncoderFilter      │
│ + encodeData  │  └──────────────────────────┘
│   (b,end):FDS │
│ + encodeTrlr  │
│   (t): FTS    │
│ + setEncFC(): │
│        void   │
└───────────────┘

        Return Status Enums:
        ┌────────────────────────────────┐
        │ FilterHeadersStatus:           │
        │ + Continue                     │
        │ + StopIteration                │
        │ + StopAllIterationAndBuffer    │
        │ + StopAllIterationAndWatermark │
        └────────────────────────────────┘

        ┌────────────────────────────────┐
        │ FilterDataStatus:              │
        │ + Continue                     │
        │ + StopIterationAndBuffer       │
        │ + StopIterationAndWatermark    │
        │ + StopIterationNoBuffer        │
        └────────────────────────────────┘


CONCRETE IMPLEMENTATIONS:

    ┌────────────────────────────────────────────┐
    │ RouterFilter                               │
    │ (concrete, implements StreamFilter)        │
    ├────────────────────────────────────────────┤
    │ - config_: RouterFilterConfigSharedPtr     │
    │ - cluster_manager_: ClusterManager&        │
    │ - upstream_request_: UpstreamRequestPtr    │
    │ - decoder_callbacks_: DecoderFilterCB*     │
    │ - encoder_callbacks_: EncoderFilterCB*     │
    │ - route_: RouteConstSharedPtr              │
    ├────────────────────────────────────────────┤
    │ + decodeHeaders(h, end): FilterHeadersS    │
    │ + decodeData(buf, end): FilterDataStatus   │
    │ + decodeTrailers(t): FilterTrailersStatus  │
    │ + encodeHeaders(h, end): FilterHeadersS    │
    │ + encodeData(buf, end): FilterDataStatus   │
    │ + encodeTrailers(t): FilterTrailersStatus  │
    └────────────────────────────────────────────┘

    ┌────────────────────────────────────────────┐
    │ JwtAuthenticationFilter                    │
    │ (concrete, implements StreamDecoderFilter) │
    ├────────────────────────────────────────────┤
    │ - config_: JwtConfigSharedPtr              │
    │ - verifier_: JwtVerifierPtr                │
    │ - callbacks_: DecoderFilterCallbacks*      │
    ├────────────────────────────────────────────┤
    │ + decodeHeaders(h, end): FilterHeadersS    │
    │ + decodeData(buf, end): FilterDataStatus   │
    │ + decodeTrailers(t): FilterTrailersStatus  │
    └────────────────────────────────────────────┘

    ┌────────────────────────────────────────────┐
    │ CorsFilter                                 │
    │ (concrete, implements StreamDecoderFilter) │
    ├────────────────────────────────────────────┤
    │ - config_: CorsConfigSharedPtr             │
    │ - callbacks_: DecoderFilterCallbacks*      │
    ├────────────────────────────────────────────┤
    │ + decodeHeaders(h, end): FilterHeadersS    │
    │ + decodeData(buf, end): FilterDataStatus   │
    └────────────────────────────────────────────┘
```

## Filter Factory Classes

### Factory Interface Hierarchy

```
┌─────────────────────────────────────────────────────────────┐
│                   FILTER FACTORY HIERARCHY                  │
└─────────────────────────────────────────────────────────────┘

                    ┌──────────────────────────┐
                    │ NamedFactory             │
                    │ (interface)              │
                    ├──────────────────────────┤
                    │ + name(): string         │
                    │ + category(): string     │
                    └──────────────┬───────────┘
                                   │
                                   │ extends
                                   │
            ┌──────────────────────┼──────────────────────┐
            │                      │                      │
            ▼                      ▼                      ▼
┌─────────────────────┐  ┌──────────────────────┐  ┌──────────────────────┐
│ NamedNetwork        │  │ NamedHttp            │  │ NamedListener        │
│ FilterConfigFactory │  │ FilterConfigFactory  │  │ FilterConfigFactory  │
│ (interface)         │  │ (interface)          │  │ (interface)          │
├─────────────────────┤  ├──────────────────────┤  ├──────────────────────┤
│ + createFilterFact  │  │ + createFilterFact   │  │ + createListenerFilt │
│   oryFromProto(     │  │   oryFromProto(      │  │   erFactoryFromProto │
│     proto,          │  │     proto,           │  │     (proto,          │
│     context):       │  │     stats_prefix,    │  │     matcher,         │
│   StatusOr<         │  │     context):        │  │     context):        │
│     FilterFactoryCb>│  │   StatusOr<          │  │   ListenerFilter     │
│                     │  │     FilterFactoryCb> │  │     FactoryCb        │
│ + isTerminalFilter  │  │                      │  │                      │
│   ByProto(): bool   │  │ + createEmpty        │  │                      │
│                     │  │   ConfigProto(): MP  │  │                      │
│                     │  │                      │  │                      │
│                     │  │ + createRouteSpe     │  │                      │
│                     │  │   cificFilterConfig  │  │                      │
│                     │  │   (): RSFC           │  │                      │
│                     │  │                      │  │                      │
│                     │  │ + isTerminalFilter   │  │                      │
│                     │  │   ByProto(): bool    │  │                      │
└─────────────────────┘  └──────────────────────┘  └──────────────────────┘


CONCRETE FACTORY IMPLEMENTATIONS:

    ┌─────────────────────────────────────────────────┐
    │ TcpProxyConfigFactory                           │
    │ (implements NamedNetworkFilterConfigFactory)    │
    ├─────────────────────────────────────────────────┤
    │ + name(): "envoy.filters.network.tcp_proxy"     │
    │ + category(): "envoy.filters.network"           │
    │ + createFilterFactoryFromProto(proto, ctx):     │
    │     StatusOr<Network::FilterFactoryCb>          │
    │ + isTerminalFilterByProto(): true               │
    └─────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────┐
    │ RouterFilterConfigFactory                       │
    │ (implements NamedHttpFilterConfigFactory)       │
    ├─────────────────────────────────────────────────┤
    │ + name(): "envoy.filters.http.router"           │
    │ + category(): "envoy.filters.http"              │
    │ + createFilterFactoryFromProto(proto, sp, ctx): │
    │     StatusOr<Http::FilterFactoryCb>             │
    │ + createEmptyConfigProto(): MessagePtr          │
    │ + isTerminalFilterByProto(): true               │
    └─────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────┐
    │ TlsInspectorConfigFactory                       │
    │ (implements NamedListenerFilterConfigFactory)   │
    ├─────────────────────────────────────────────────┤
    │ + name(): "envoy.filters.listener.tls_insp..."  │
    │ + category(): "envoy.filters.listener"          │
    │ + createListenerFilterFactoryFromProto(...):    │
    │     Network::ListenerFilterFactoryCb            │
    └─────────────────────────────────────────────────┘


FACTORY CALLBACK TYPES:

    ┌──────────────────────────────────────────┐
    │ Network::FilterFactoryCb                 │
    │ = std::function<void(FilterManager&)>    │
    └──────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────┐
    │ Http::FilterFactoryCb                                │
    │ = std::function<void(FilterChainFactoryCallbacks&)>  │
    └──────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────────┐
    │ Network::ListenerFilterFactoryCb                         │
    │ = std::function<void(ListenerFilterManager&)>            │
    └──────────────────────────────────────────────────────────┘
```

## Filter Chain Classes

### Filter Chain Structure

```
┌─────────────────────────────────────────────────────────────┐
│                   FILTER CHAIN CLASSES                      │
└─────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────┐
    │ FilterChain                                    │
    │ (interface)                                    │
    ├────────────────────────────────────────────────┤
    │ + transportSocketFactory(): DTSF&              │
    │ + networkFilterFactories(): NFF&               │
    │ + transportSocketConnectTimeout(): duration    │
    │ + name(): string_view                          │
    │ + metadata(): Metadata&                        │
    └──────────────────────┬─────────────────────────┘
                           │
                           │ implemented by
                           ▼
    ┌────────────────────────────────────────────────┐
    │ FilterChainImpl                                │
    │ (concrete)                                     │
    ├────────────────────────────────────────────────┤
    │ - transport_socket_factory_: DTSF_Ptr          │
    │ - network_filter_factories_: vector<           │
    │     FilterConfigProvider<FilterFactoryCb>>     │
    │ - transport_socket_connect_timeout_: duration  │
    │ - name_: string                                │
    │ - metadata_: Metadata                          │
    │ - filter_chain_context_: FCContext_Ptr         │
    ├────────────────────────────────────────────────┤
    │ + transportSocketFactory(): DTSF&              │
    │ + networkFilterFactories(): NFF&               │
    │ + transportSocketConnectTimeout(): duration    │
    │ + name(): string_view                          │
    │ + metadata(): Metadata&                        │
    │ + setFilterChainFactoryContext(ctx): void      │
    └────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────┐
    │ FilterChainManager                             │
    │ (interface)                                    │
    ├────────────────────────────────────────────────┤
    │ + findFilterChain(socket, info): FilterChain* │
    │ + filterChains(): vector<FilterChain*>         │
    └──────────────────────┬─────────────────────────┘
                           │
                           │ implemented by
                           ▼
    ┌────────────────────────────────────────────────┐
    │ FilterChainManagerImpl                         │
    │ (concrete)                                     │
    ├────────────────────────────────────────────────┤
    │ - destination_ips_: hashmap<IP,                │
    │     FilterChainsByDestinationPort>             │
    │ - destination_ip_tries_: vector<               │
    │     pair<CidrRange, FCByDestPort>>             │
    │ - default_filter_chain_: FilterChainPtr        │
    ├────────────────────────────────────────────────┤
    │ + findFilterChain(socket, info): FilterChain* │
    │ + filterChains(): vector<FilterChain*>         │
    │ - findDestinationIP(ip): FCByDestPort*         │
    │ - findDestinationPort(prev, port): FCByServN*  │
    │ - findServerName(prev, sni): FCByTransport*    │
    │ - findTransportProtocol(prev, tp): FCByApp*    │
    │ - findApplicationProtocols(prev, alpn): FC*    │
    └────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────┐
    │ FilterChainFactory                             │
    │ (interface)                                    │
    ├────────────────────────────────────────────────┤
    │ + createNetworkFilterChain(conn, factories):   │
    │     bool                                       │
    └──────────────────────┬─────────────────────────┘
                           │
                           │ implemented by
                           ▼
    ┌────────────────────────────────────────────────┐
    │ ListenerImpl                                   │
    │ (concrete)                                     │
    ├────────────────────────────────────────────────┤
    │ - filter_chain_manager_: FCManager_Ptr         │
    │ - listener_config_: ListenerConfig&            │
    ├────────────────────────────────────────────────┤
    │ + createNetworkFilterChain(conn, factories):   │
    │     bool                                       │
    └────────────────────────────────────────────────┘

RELATIONSHIPS:

    ListenerImpl ───uses──► FilterChainManager
                 ───uses──► FilterChain
                 ───implements──► FilterChainFactory

    FilterChainManagerImpl ───contains──► FilterChainImpl (many)

    FilterChainImpl ───contains──► FilterFactoryCb (many)
                    ───uses──► DownstreamTransportSocketFactory
                    ───uses──► FilterChainFactoryContext
```

## Filter Manager Classes

### Filter Manager Structure

```
┌─────────────────────────────────────────────────────────────┐
│                   FILTER MANAGER CLASSES                    │
└─────────────────────────────────────────────────────────────┘

NETWORK FILTER MANAGER:

    ┌────────────────────────────────────────────────┐
    │ Network::FilterManager                         │
    │ (interface)                                    │
    ├────────────────────────────────────────────────┤
    │ + addReadFilter(filter): void                  │
    │ + addWriteFilter(filter): void                 │
    │ + addFilter(filter): void                      │
    │ + initializeReadFilters(): void                │
    │ + onRead(): FilterStatus                       │
    │ + onWrite(): FilterStatus                      │
    └──────────────────────┬─────────────────────────┘
                           │
                           │ implemented by
                           ▼
    ┌────────────────────────────────────────────────┐
    │ FilterManagerImpl                              │
    │ (concrete)                                     │
    ├────────────────────────────────────────────────┤
    │ - connection_: Connection&                     │
    │ - upstream_filters_: list<ActiveReadFilter>    │
    │ - downstream_filters_: list<ActiveWriteFilter> │
    │ - current_read_filter_: iterator               │
    │ - current_write_filter_: iterator              │
    ├────────────────────────────────────────────────┤
    │ + addReadFilter(filter): void                  │
    │ + addWriteFilter(filter): void                 │
    │ + addFilter(filter): void                      │
    │ + initializeReadFilters(): void                │
    │ + onRead(): FilterStatus                       │
    │ + onWrite(): FilterStatus                      │
    └────────────────────────────────────────────────┘

    Nested Structure:
    ┌────────────────────────────────────────────────┐
    │ FilterManagerImpl::ActiveReadFilter            │
    ├────────────────────────────────────────────────┤
    │ - filter_: ReadFilterSharedPtr                 │
    │ - initialized_: bool                           │
    └────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────┐
    │ FilterManagerImpl::ActiveWriteFilter           │
    ├────────────────────────────────────────────────┤
    │ - filter_: WriteFilterSharedPtr                │
    │ - initialized_: bool                           │
    └────────────────────────────────────────────────┘


HTTP FILTER MANAGER:

    ┌────────────────────────────────────────────────────┐
    │ Http::FilterChainFactoryCallbacks                 │
    │ (interface)                                        │
    ├────────────────────────────────────────────────────┤
    │ + addStreamDecoderFilter(filter): void             │
    │ + addStreamEncoderFilter(filter): void             │
    │ + addStreamFilter(filter): void                    │
    │ + addAccessLogHandler(handler): void               │
    └──────────────────────────┬─────────────────────────┘
                               │
                               │ implemented by
                               ▼
    ┌────────────────────────────────────────────────────┐
    │ Http::FilterManager                                │
    │ (concrete, also implements                         │
    │  FilterChainFactoryCallbacks)                      │
    ├────────────────────────────────────────────────────┤
    │ - decoder_filters_: list<ActiveStreamDecoderFilter>│
    │ - encoder_filters_: list<ActiveStreamEncoderFilter>│
    │ - current_decoder_filter_: iterator                │
    │ - current_encoder_filter_: reverse_iterator        │
    │ - buffering_: bool                                 │
    ├────────────────────────────────────────────────────┤
    │ + addStreamDecoderFilter(filter): void             │
    │ + addStreamEncoderFilter(filter): void             │
    │ + addStreamFilter(filter): void                    │
    │ + decodeHeaders(headers, end): void                │
    │ + decodeData(data, end): void                      │
    │ + decodeTrailers(trailers): void                   │
    │ + encodeHeaders(headers, end): void                │
    │ + encodeData(data, end): void                      │
    │ + encodeTrailers(trailers): void                   │
    │ + continueDecoding(): void                         │
    │ + continueEncoding(): void                         │
    └────────────────────────────────────────────────────┘

    Nested Structure:
    ┌──────────────────────────────────────────────────────┐
    │ FilterManager::ActiveStreamDecoderFilter             │
    ├──────────────────────────────────────────────────────┤
    │ - filter_: StreamDecoderFilterSharedPtr              │
    │ - iteration_state_: FilterIterationState             │
    │ - headers_continued_: bool                           │
    └──────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────┐
    │ FilterManager::ActiveStreamEncoderFilter             │
    ├──────────────────────────────────────────────────────┤
    │ - filter_: StreamEncoderFilterSharedPtr              │
    │ - iteration_state_: FilterIterationState             │
    │ - headers_continued_: bool                           │
    └──────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────┐
    │ FilterIterationState (enum)                          │
    ├──────────────────────────────────────────────────────┤
    │ + Continue                                           │
    │ + StopIteration                                      │
    └──────────────────────────────────────────────────────┘
```

## Registry System Classes

### Registry Structure

```
┌─────────────────────────────────────────────────────────────┐
│                    REGISTRY SYSTEM                          │
└─────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────┐
    │ Registry::FactoryRegistry<Base>                │
    │ (template class)                               │
    ├────────────────────────────────────────────────┤
    │ + registerFactory(factory, name): void (static)│
    │ + getFactory(name): Base* (static)             │
    │ + allFactories(): vector<Base*> (static)       │
    ├────────────────────────────────────────────────┤
    │ - factories(): hashmap<string, Base*> (static) │
    └────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────┐
    │ Registry::RegisterFactory<Factory, Base>       │
    │ (template class)                               │
    ├────────────────────────────────────────────────┤
    │ + RegisterFactory() (constructor)              │
    │   └─► Registers Factory with FactoryRegistry  │
    └────────────────────────────────────────────────┘


USAGE EXAMPLE:

    Static Registration:
    ┌─────────────────────────────────────────────────────┐
    │ // In filter implementation file                    │
    │                                                     │
    │ REGISTER_FACTORY(TcpProxyConfigFactory,            │
    │                  NamedNetworkFilterConfigFactory); │
    │                                                     │
    │ Expands to:                                         │
    │ static Registry::RegisterFactory<                  │
    │     TcpProxyConfigFactory,                         │
    │     NamedNetworkFilterConfigFactory>               │
    │   TcpProxyConfigFactory_registered;                │
    └─────────────────────────────────────────────────────┘

    Runtime Lookup:
    ┌─────────────────────────────────────────────────────┐
    │ auto* factory = Registry::FactoryRegistry<          │
    │     NamedNetworkFilterConfigFactory>::getFactory(   │
    │         "envoy.filters.network.tcp_proxy");         │
    │                                                     │
    │ if (factory != nullptr) {                           │
    │   auto factory_cb = factory->                       │
    │       createFilterFactoryFromProto(...);            │
    │ }                                                   │
    └─────────────────────────────────────────────────────┘

REGISTRY INSTANTIATIONS:

    ┌──────────────────────────────────────────────────────┐
    │ HttpFilterRegistry                                   │
    │ = FactoryRegistry<NamedHttpFilterConfigFactory>      │
    └──────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────┐
    │ NetworkFilterRegistry                                │
    │ = FactoryRegistry<NamedNetworkFilterConfigFactory>   │
    └──────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────┐
    │ ListenerFilterRegistry                               │
    │ = FactoryRegistry<NamedListenerFilterConfigFactory>  │
    └──────────────────────────────────────────────────────┘
```

## Complete System Overview

### End-to-End Class Relationships

```
┌──────────────────────────────────────────────────────────────────────────┐
│                      COMPLETE SYSTEM OVERVIEW                            │
└──────────────────────────────────────────────────────────────────────────┘

CONFIGURATION PHASE:

    User Config (YAML)
           │
           ▼
    ┌──────────────────┐
    │ ListenerManager  │
    └────────┬─────────┘
             │ creates
             ▼
    ┌──────────────────┐        ┌──────────────────────────┐
    │ ListenerImpl     │───uses─►│ FilterChainManagerImpl   │
    └────────┬─────────┘        └────────┬─────────────────┘
             │                           │ contains many
             │                           ▼
             │                  ┌──────────────────┐
             │                  │ FilterChainImpl  │
             │                  └────────┬─────────┘
             │                           │ contains
             │                           ▼
             │                  ┌───────────────────────┐
             │                  │ FilterFactoryCb list  │
             │                  └───────────────────────┘
             │                           ▲
             │                           │ created by
             │                           │
    ┌────────┴────────────────────┐    │
    │ Registry lookup:            │    │
    │ getFactory(filter_name)     ├────┘
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────────┐
    │ NamedFilterConfigFactory        │
    │ ::createFilterFactoryFromProto()│
    └─────────────────────────────────┘


RUNTIME PHASE (Network):

    New Connection
           │
           ▼
    ┌──────────────────┐
    │ ListenerImpl     │
    │ ::onAccept()     │
    └────────┬─────────┘
             │ runs listener filters
             ▼
    ┌──────────────────────────┐
    │ ListenerFilterManager    │
    │ (TLS Inspector, etc.)    │
    └────────┬─────────────────┘
             │ extracts SNI, ALPN
             ▼
    ┌──────────────────────────┐
    │ FilterChainManager       │
    │ ::findFilterChain()      │
    └────────┬─────────────────┘
             │ returns selected chain
             ▼
    ┌──────────────────┐
    │ FilterChain      │
    └────────┬─────────┘
             │ provides factories
             ▼
    ┌──────────────────────────┐
    │ FilterChainFactory       │
    │ ::createNetworkFC()      │
    └────────┬─────────────────┘
             │ invokes callbacks
             ▼
    ┌──────────────────────────┐
    │ FilterFactoryCb()        │
    │ (creates filter)         │
    └────────┬─────────────────┘
             │ adds to
             ▼
    ┌──────────────────────────┐       ┌──────────────────┐
    │ FilterManager            │◄──────┤ Filter instances │
    │ (manages iteration)      │       └──────────────────┘
    └────────┬─────────────────┘
             │ processes
             ▼
    ┌──────────────────────────┐
    │ Connection data          │
    └──────────────────────────┘


RUNTIME PHASE (HTTP):

    New HTTP Stream
           │
           ▼
    ┌──────────────────────────┐
    │ HttpConnectionManager    │
    │ (network filter)         │
    └────────┬─────────────────┘
             │ creates stream
             ▼
    ┌──────────────────────────┐
    │ FilterChainHelper        │
    │ ::createFilterChain()    │
    └────────┬─────────────────┘
             │ invokes callbacks
             ▼
    ┌──────────────────────────┐
    │ Http::FilterFactoryCb()  │
    │ (creates filter)         │
    └────────┬─────────────────┘
             │ adds to
             ▼
    ┌──────────────────────────┐       ┌──────────────────┐
    │ Http::FilterManager      │◄──────┤ HTTP Filter      │
    │ (manages iteration)      │       │ instances        │
    └────────┬─────────────────┘       └──────────────────┘
             │ processes
             ▼
    ┌──────────────────────────┐
    │ HTTP request/response    │
    └──────────────────────────┘
```

## Summary

This document provided UML class diagrams showing:

1. **Factory Context Hierarchy**: Inheritance chain from CommonFactoryContext to FilterChainFactoryContext
2. **Filter Interfaces**: Network and HTTP filter interface hierarchies
3. **Filter Factories**: Factory class structure and registration
4. **Filter Chains**: FilterChain and FilterChainManager classes
5. **Filter Managers**: Network and HTTP filter manager structures
6. **Registry System**: Factory registration and lookup mechanism
7. **Complete Overview**: End-to-end relationships from config to runtime

## Next Steps

Proceed to **Part 10: Sequence Diagrams and Flow Charts** to see the complete flow with timing and interactions.

---

**Document Version**: 1.0
**Last Updated**: 2026-02-28
**Related Code Paths**: All paths covered in previous documents
