diff --git a/conf.sh b/conf.sh
index c361fbc..da24702 100755
--- a/conf.sh
+++ b/conf.sh
@@ -4,6 +4,14 @@
 
 # Build UADK into static library
 COMPILE_TYPE="--disable-static --enable-shared"
+CC=/usr/bin/gcc
+CFLAGS="-O2"
+LDFLAGS=
+
+EXTRA_ARGS=()
+for arg in "$@"; do
+  EXTRA_ARGS+=("$arg")
+done
 
 # These two parameters could be in arbitary sequence
 if [[ $1 && $1 = "--static" ]] || [[ $2 && $2 = "--static" ]]; then
@@ -17,4 +25,5 @@ ac_cv_func_malloc_0_nonnull=yes ac_cv_func_realloc_0_nonnull=yes ./configure \
 	--host aarch64-linux-gnu \
 	--target aarch64-linux-gnu \
 	--includedir=/usr/local/include/ \
-	$COMPILE_TYPE
+	$COMPILE_TYPE \
+	"${EXTRA_ARGS[@]}"
