diff -uNr nghttp2-1.69.0/CMakeLists.txt nghttp2-1.69.0-ssize/CMakeLists.txt
--- nghttp2-1.69.0/CMakeLists.txt	2026-04-19 09:17:31.596992056 +0000
+++ nghttp2-1.69.0-ssize/CMakeLists.txt	2026-07-23 06:57:24.366378787 +0000
@@ -353,7 +353,11 @@
 if(SIZEOF_SSIZE_T STREQUAL "")
   # ssize_t is a signed type in POSIX storing at least -1.
   # Set it to "int" to match the behavior of AC_TYPE_SSIZE_T (autotools).
-  set(ssize_t int)
+  if(WIN32 AND CMAKE_SIZEOF_VOID_P EQUAL 8)
+    set(ssize_t ptrdiff_t)
+  else()
+    set(ssize_t int)
+  endif()
 endif()
 # AC_TYPE_UINT8_T
 # AC_TYPE_UINT16_T
